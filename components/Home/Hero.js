import React from "react";

const Hero = () => {
  return (
    <div className="hero">
      <h1>{data.Hero.Headline}</h1>
      <h2>{data.Hero.Subheading}</h2>
    </div>
  );
};

export default Hero;
